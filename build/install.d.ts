import type { App } from "vue";
export interface IEeasemobOptions {
    appKey: string;
}
declare const _default: {
    install: (app: App, options: IEeasemobOptions) => void;
};
export default _default;
